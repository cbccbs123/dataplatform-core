"""맹검 판정의 판정 기준(루브릭) — 버전 매긴 상수.

**흐름에서의 위치**: 판정 러너(``scripts/judge_relations.py``)가 이 문자열을 프롬프트에 넣고,
**같은 문자열을 판정 산출물의 ``rubric_text`` 에 그대로 복사**한다. 한 곳에서 읽어 양쪽에 쓰는
이유는, 프롬프트에 실제로 들어가는 문구와 문서가 갈라지면 둘 중 하나는 반드시 낡기 때문이다.

**설계 판단**: `.md` 문서가 아니라 파이썬 상수로 둔다. 문서로 두면 "프롬프트가 실제로 무엇을
물었는가"를 사후에 확인할 방법이 없다.

**깨지면 안 되는 것**: 문구를 고치면 ``RUBRIC_VERSION`` 과 ``RUBRIC_KO_V1_SHA256`` 을 **함께**
올린다. 버전이 같은데 문구가 다르면 과거 판정과의 비교가 조용히 무효가 된다.
"""
from __future__ import annotations

RUBRIC_VERSION = "v1"

# 2026-07-07·07-10·07-28 세 차례 판정에서 실제로 쓴 기준을 문자열로 고정한 것이다.
# (지금까지는 어디에도 기록되지 않아 "이 측정이 무엇을 쟀는가"를 사후에 알 수 없었다.)
RUBRIC_KO_V1 = """판정 기준(정확히 셋 중 하나):
- "strong": **같은 구체적 대상**을 다룬다(같은 사건·장소·인물·요리·제품 등). 서로 다른 매체여도 대상이 같으면 strong.
- "weak": 같은 **넓은 분야**일 뿐 구체 대상이 다르다(둘 다 스포츠지만 야구와 배드민턴, 둘 다 물리학이지만 다른 주제).
- "none": 무관하거나, 한쪽이 무내용(검은 화면·로고·플레이스홀더·구독 요청 등)이다."""

# 문구 잠금 — 테스트가 이 값과 실제 해시를 대조한다(위 "깨지면 안 되는 것" 참조).
RUBRIC_KO_V1_SHA256 = "a01ca8fae37ea504f94b3e8c3e501b0ccfab3b6798bf87e3039dd8d8bc907f81"
